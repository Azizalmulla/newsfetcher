# NewsFetcher API

Phase 0 FastAPI baseline for the Kuwait media-monitoring platform.

## Local commands

From repository root:

```bash
make bootstrap
make up
make migrate
make seed
make test
```

Health: `GET /health`  
Readiness: `GET /ready`
